[package]
name = "cx"
version = "0.1.0"
edition = "2024"
description = "Conditional eXecution for linewise just commands"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
shell-words = "1.1"

[dev-dependencies]
tempfile = "3.10"
